// 自动生成的 Bundle 引用。
/** workshop 的资源包引用；openBundle 只准备包，内部资源和配置仍按需加载。 */
export const WorkshopBundles = {
    /** default 资源包，Bundle ID 为 m-workshop；可作为 config.load 的 bundle 选项。 */
    default: { id: 'm-workshop' },
} as const;
