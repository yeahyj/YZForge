// 自动生成的 Bundle 引用。
/** showcase 的资源包引用；openBundle 只准备包，内部资源和配置仍按需加载。 */
export const ShowcaseBundles = {
    /** default 资源包，Bundle ID 为 m-showcase；可作为 config.load 的 bundle 选项。 */
    default: { id: 'm-showcase' },
    /** extra 资源包，Bundle ID 为 showcase-extra；可作为 config.load 的 bundle 选项。 */
    extra: { id: 'showcase-extra' },
} as const;
