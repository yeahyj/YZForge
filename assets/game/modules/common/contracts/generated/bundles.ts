// 自动生成的 Bundle 引用。
/** common 的资源包引用；openBundle 只准备包，内部资源和配置仍按需加载。 */
export const CommonBundles = {
    /** default 资源包，Bundle ID 为 m-common；可作为 config.load 的 bundle 选项。 */
    default: { id: 'm-common' },
} as const;
